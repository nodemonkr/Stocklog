# StockLog v3.60.4

## 투자성향 테스트 UX 개편

- 기존 30문항과 점수 산정 로직은 변경하지 않음
- 회원가입 투자성향 검사와 로그인 후 투자성향 분석 페이지 UI를 동일한 퀴즈형 흐름으로 통일
- 한 화면에 한 문항만 집중해서 표시
- 선택지를 큰 카드형 버튼으로 변경하고 선택 상태를 더 강하게 표시
- 답변 선택 후 약 0.26초 뒤 다음 문항으로 자동 이동
- 30문항을 6문항씩 5개 진행 단계(시작/탐색/균형/정교화/마무리)로 시각화
- 마지막 단계에 `거의 다 왔어요` 안내
- 이전 문항으로 돌아가 답변 수정 가능
- `처음부터` 버튼으로 새 문항 순서로 다시 시작 가능
- 마지막 문항에서는 `내 투자성향 확인하기` 버튼 표시
- 진행 중 답변/문항 순서를 sessionStorage에 임시 저장
  - 로그인 후 투자성향 테스트는 페이지를 나갔다 돌아와도 진행 상태 복구
  - 일반 회원가입은 비밀번호를 저장하지 않으며, 새로고침 시 가입정보를 다시 확인한 뒤 기존 투자성향 응답을 이어서 진행
- 모바일에서도 카드 선택 영역과 단계 표시가 충분히 크게 보이도록 반응형 UI 개선
