# StockLog v3.60.5

- 투자 성향 결과가 DB에 존재하면 브라우저 임시 답변보다 결과 페이지를 우선 표시하도록 수정
- 저장된 결과가 있는 회원은 `투자 성향 분석` 메뉴 진입 시 `나의 투자 성향` 페이지 표시
- 재검사는 사용자가 `다시 테스트`를 직접 선택했을 때만 시작
- 일반 회원가입 폼 실시간 검증 UI 추가
- 아이디 형식 + 서버 중복 여부 350ms debounce 자동 확인
- 비밀번호 8자 이상 / 비밀번호 확인 일치 자동 검사
- 이름 / 성별 / 생년월일 / 만 14세 / 휴대폰 형식 자동 검사
- 휴대폰 번호 입력 중 하이픈 자동 정리
- 필수 약관 동의 상태 시각화
- 정상 항목 초록색, 오류 항목 빨간색, 아이디 확인 중 보라색 표시
- 모든 필수 항목이 유효해야 회원가입 `다음` 버튼 활성화
- 백엔드 `/api/auth/check-username` 추가 및 최종 회원가입 검증은 기존대로 유지
