# StockLog v3.76.3

## 자동매매 이력 의미 정리

- `매수 · 매도` 탭을 `실제 주문` 탭으로 변경했습니다.
- 실제 키움 주문 전송 단계에 진입한 `submitting/accepted/partial/filled/order_failed/cancelled` 건만 주문 이력에 표시합니다.
- Gbot이 BUY/SELL 의견을 냈더라도 서버 안전장치에서 사전 차단된 `blocked` 판단은 실제 주문 이력에서 제외합니다.
- 안전장치 차단 판단은 `전체 판단`에서만 확인할 수 있고 `주문 미전송`, `안전장치 사전 차단 · 키움 주문 미전송`으로 명확하게 표시합니다.
- API 응답에 `order_attempted`를 추가해 프론트가 AI 판단과 실제 브로커 주문 시도를 구분합니다.
- 기존 주문 안전장치 자체는 그대로 유지되며, 차단된 BUY/SELL은 키움 주문 함수가 호출되기 전에 종료됩니다.
