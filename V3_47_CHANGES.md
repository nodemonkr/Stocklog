# StockLog v3.47 변경사항

## 모의투자 포트폴리오

- 키움 주문가능금액 직접 조회 API를 추가했습니다.
  - `kt00010` 주문인출가능금액의 `ord_alowa`를 우선 사용합니다.
  - 모의계좌에서 해당 호출이 응답하지 않는 경우 `kt00001` 예수금상세현황의 100% 주문가능금액을 fallback으로 사용합니다.
  - 실시간 조회가 일시적으로 실패하면 임의 금액을 만들지 않고 마지막으로 키움에서 확인된 금액만 `stale` 상태로 표시합니다.
  - 모의투자 페이지 진입 시 즉시 조회하며 화면이 활성화된 동안 15초 간격으로 갱신합니다. 주문 체결 요청 후에도 재조회합니다.

- 상단 상태 스트립을 단순화했습니다.
  - 가격/평가손익/주문 관련 긴 안내 문구를 제거했습니다.
  - `최근 실시간 수신` 시간만 표시합니다.

- 보유종목 마스터/상세 레이아웃을 재설계했습니다.
  - 보유종목이 1~2개여도 좌우 카드 높이가 어색하게 끊기지 않도록 desktop에서 동일 높이 구조를 사용합니다.
  - 태블릿/모바일에서는 자연스럽게 1열 카드 구조로 전환합니다.
  - 보유종목 각 행에 빠른 `매도` 버튼을 추가했습니다.
  - 빠른 매도 클릭 시 해당 종목을 선택하고 주문 패널을 `매도`로 전환하며 보유수량을 기본 수량으로 채웁니다.

- 보유종목 상세 차트를 개선했습니다.
  - DB에 저장된 실제 최근 일봉을 항상 fallback 차트로 표시합니다.
  - 실시간 체결가가 수신되면 저장 일봉의 마지막 가격에 현재가를 반영합니다.
  - 저장된 실제 데이터가 전혀 없을 때만 데이터 없음 상태를 표시합니다.

- 오늘 투자자별 수급을 추가했습니다.
  - 키움 `ka10060` 실제 데이터를 사용합니다.
  - 외국인, 기관계의 매수/매도/순매수를 크게 표시합니다.
  - 금융투자, 투신, 연기금 등, 보험, 은행, 사모펀드의 매수/매도/순매수를 함께 표시합니다.
  - 동일 종목 반복 조회로 키움 호출이 과도해지지 않도록 90초 캐시를 적용했습니다.

## 주식 주문

- 주문 UI의 작은 텍스트, 입력값, 탭, 호가표, 버튼 크기와 대비를 개선했습니다.
- 지정가 주문 가격 입력에 `KRW · 원` 및 입력 우측 `원` 단위를 명확히 표시합니다.
- 기존 `매도1` / `매수1` 단축 버튼을 제거했습니다.
- 실제 호가 API의 최우선 매도호가 / 최우선 매수호가 가격을 표시합니다.
- 현재가, 최우선 매도호가, 최우선 매수호가 버튼을 누르면 지정가 가격에 바로 입력됩니다.
- 예약주문 가격 선택에도 같은 실제 호가 단축 버튼을 적용했습니다.

## 전역 가독성

- 사이트 전체 CSS의 명시적 px 폰트 크기 하한을 12px로 높였습니다.
- 일반 본문은 15~16px 중심으로 유지하고 큰 제목은 과도하게 키우지 않았습니다.
- 라벨, 표, 보조문구, 버튼, 입력창 등 기존에 지나치게 작던 글씨를 전반적으로 확대했습니다.

## 버전

- API / UI 버전: v3.47
