# StockLog v3.49 변경사항

## 사용자 정보 카드
- 좌측 상단 계정 카드를 간결한 구조로 재정리했습니다.
- `@아이디`, `접속 중` 문구, 접속 상태 점을 제거했습니다.
- 이름/계정 유형/AI 이용 상태만 우선 노출합니다.
- 카드 하단에 `프로필`, `로그아웃`을 배치했습니다.
- 좁은 사이드바에서는 프로필/로그아웃을 아이콘 버튼으로 유지합니다.

## 프로필 샘플 페이지
- 기존 `투자 성향 분석`과 별개의 계정 `프로필` 페이지 샘플을 추가했습니다.
- 기본 정보, 소셜 계정 관리 자리, 개인 설정 영역을 구성했습니다.
- 실제 프로필 편집 및 소셜 연결/해제는 추후 확장할 수 있도록 비활성 샘플 상태입니다.

## restart-all.sh production build 자동화
- `/home/conda/Stocklog/restart-all.sh` 실행 시 서비스 중지 전에 `frontend` production build를 먼저 수행합니다.
- `node_modules`가 없거나 `package-lock.json`이 바뀌었으면 `npm ci`로 의존성을 복구합니다.
- `npm run build` 성공 및 `frontend/dist/index.html` 생성을 확인한 뒤 기존 프로세스를 재시작합니다.
- 빌드 실패 시 기존 서비스를 먼저 내리지 않아 마지막 정상 dist를 보존합니다.

## 배포 압축 구조
- 배포 압축파일의 최상위 폴더는 항상 `Stocklog/`입니다.
- 압축 해제 후 바로 `Stocklog/restart-all.sh`를 사용할 수 있습니다.
