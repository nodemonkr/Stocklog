# StockLog v3.72.0

## 프리미엄 AI 이중 분석 체계

- 프리미엄/이벤트/관리자 상세 AI 분석은 **StockLog Gbot + StockLog Obot** 두 분석 엔진의 독립 의견을 모두 확보한 뒤 완료됩니다.
- 두 엔진은 같은 StockLog 정량 컨텍스트를 서로의 의견 없이 독립적으로 분석합니다.
- 둘의 의견이 일치/불일치하는지 표시하고, StockLog가 재무·밸류에이션·수급·추세·뉴스·리포트·공시 정량 근거와 다시 대조해 최종 의견을 생성합니다.
- 한쪽 엔진이 실패하면 1회 자동 재시도합니다.
- 프리미엄 분석에서는 한쪽만 성공한 결과나 deterministic fallback을 정식 완료 결과로 표시하지 않습니다.
- 최종 종합 단계만 실패한 경우에는 이미 완료된 Gbot/Obot 두 독립 결과와 StockLog 정량 엔진을 이용한 안전한 합의 로직으로 결과를 구성합니다.

## 분석 결과 UI

- 최근 프리미엄 분석 결과에 `StockLog Gbot · Obot 교차 분석` 영역을 추가했습니다.
- Gbot과 Obot 각각의 독립 판단, 분석 확신도, 요약, 핵심 긍정 근거와 주의 근거를 별도 카드로 확인할 수 있습니다.
- 두 봇이 같은 의견인지 다른 의견인지 표시하고, 의견이 다를 경우 StockLog 정량 재검증이 이루어졌음을 안내합니다.
- 기존 최종 의견, 정량 비교, 신규/보유자 전략, 긍정/부정 근거, 판단 변경 조건은 두 분석 이후의 최종 종합 결과를 사용합니다.

## 사용자 노출 보호

- 사용자용 분석 응답과 상태 API에서 실제 외부/내부 AI 공급사명 및 모델명을 노출하지 않습니다.
- 사용자에게는 `StockLog Gbot`, `StockLog Obot` 제품명만 표시합니다.
- `/api/ai/status` 또한 실제 공급사/모델 정보 대신 Gbot/Obot 준비 상태만 반환합니다.
- 내부 운영용 공급사 설정과 서버 로그에는 장애 분석을 위한 실제 연결 정보를 유지합니다.

## 캐시/안정성

- 기존 완료 분석 캐시는 그대로 열 수 있지만, 프리미엄 계정의 과거 단일 분석 결과는 새 Gbot+Obot 교차 분석 대상임을 stale 상태로 판단합니다.
- 이미 저장된 정상 dual 결과가 있을 때 단일 엔진 fallback 결과가 공유 캐시를 덮어쓰지 못하게 했습니다.
- 새 프리미엄 분석에서는 Gbot/Obot 중 하나가 끝내 실패하면 성공으로 위장하지 않고 재시도를 안내합니다.

## 버전

- Backend API: 3.72.0
- Frontend: 3.72.0
