> **SUPERSEDED BY `mobile/V3_78_0_CHANGES.md` (2026-08-27).** 아래 누락 목록은 과거 이식 상태 기록입니다. 현재 구현 여부는 3.78.0 문서를 우선합니다.

# StockLog 모바일 네이티브 적용 현황

## 네이티브 적용 완료

- 로그인 / 로그아웃 / 로그인 유지(SecureStore)
- 로컬 회원가입 + 30문항 투자성향
- Google / Kakao / Naver 소셜 로그인 및 최초 가입 완료
- 홈: 회원등급, 시장지표, 모의투자 총자산, 보유종목
- 종목 검색
- 종목 상세: 가격, 간단 추세차트, 핵심 지표, 테마, 재무, 수급, 뉴스/리포트/공시, AI 분석 실행/상태/결과
- 스마트 분석
- 인기 테마 + GBOT 테마 요약
- 수급 분석
- 키움 모의투자 App Key / Secret Key 저장, 계좌 자동 조회, 연결 테스트
- 포트폴리오 / 보유종목 / 최근 주문 / 시장가·지정가 매수·매도
- 가격감시 예약주문 등록 / 목록 / 취소
- GBOT 자동매매 상태 / 시작 / 중지 / 1회 실행 / 핵심 안전한도 / 보유종목 / 주문 이력
- 투자성향 조회 / 재검사
- 관리자 운영 현황 / 종목 점수 갱신 / 통합 동기화 시작·중지
- 관리자 회원 검색 / 등급 변경 / 계정 삭제
- 관리자 외부 API(Naver/OpenDART/Gemini) 설정 / 테스트
- 관리자 소셜 OAuth 설정 / 실제 OAuth 테스트
- 관리자 회원 등급별 기능 권한 / 자동 새로고침 정책
- 관리자 전체 동기화 자동 일정 설정

## WebView 제거

`react-native-webview`, 내장 HTML 번들, `stocklog-bundle.ts`, `sync:web` 실행 경로를 제거했습니다.
앱은 React Native UI가 FastAPI API를 직접 호출합니다.

## 서버 연결

- 실제 FastAPI 프로세스: `8100`
- 앱 기본 연결: `http://somensomes.iptime.org:8100`
- 네트워크 fallback: `http://somensomes.iptime.org:3000`

3000은 백엔드 포트가 아닙니다. 기존 웹 진입점/프록시 경로입니다. 모바일 앱은 8100 직접 연결을 우선합니다.

## 이번 네이티브 버전에서 아직 별도 모바일 UI로 만들지 않은 저빈도 기능

백엔드 API는 그대로 유지되지만 다음 데스크톱 관리자/진단 도구는 전용 모바일 화면을 아직 만들지 않았습니다.

1. 관리자 동기화 오류 로그 ZIP/개별 파일 다운로드
2. 관리자 회원 1명에 대한 전체 상세 진단(보유계좌/동의/감사 데이터 전체를 한 화면에서 보는 데스크톱 상세 패널)
3. 테마 DB repair/normalize/개별 종목 theme diagnostic 같은 유지보수 전용 세부 도구
4. 동기화 종류별(Kiwoom/DART/theme/classification/flow) 모든 개별 시작·중지 패널 — 모바일에서는 통합 동기화와 일정 설정을 우선 제공
5. 종목 호가창(orderbook) 전용 화면
6. 예약주문 기존 항목 편집 — 등록/취소는 가능하며 수정은 현재 취소 후 재등록 방식
7. GBOT 전체 판단 감사 로그의 모든 세부 evidence/guard context — 최근 실제 주문 이력과 핵심 사유는 표시

이 항목들은 일반 회원의 핵심 투자/분석/자동매매 흐름과는 분리된 운영·진단 기능입니다.
