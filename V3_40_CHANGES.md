# StockLog v3.40.0

## 관리자 외부 API 관리

- 관리자 페이지에 **네이버 뉴스 검색 API**와 **OpenDART API** 연동 영역을 추가했습니다.
- 네이버 `Client ID / Client Secret`, DART `API Key`를 관리자 페이지에서 저장·교체·삭제할 수 있습니다.
- 저장값은 기존 StockLog Fernet 암호화 모듈을 사용해 MySQL `external_api_credentials` 테이블에 암호화 저장합니다.
- 비밀값은 프론트엔드로 되돌려 보내지 않고 마스킹된 상태와 설정 여부만 표시합니다.
- 저장 후 각 공급자별 **연결 테스트**를 관리자 페이지에서 바로 실행할 수 있습니다.
- DB 설정이 없을 때는 기존 `.env` 값을 fallback으로 사용하므로 기존 설치도 깨지지 않습니다.

## API 사용량 모니터링

신규 테이블:

- `api_usage_daily`: 일별 공급자 호출 집계
- `api_usage_logs`: 개별 호출/오류 기록

관리자 화면에서 다음 값을 확인할 수 있습니다.

### 네이버 뉴스

- 오늘 사용량 / 25,000
- 오늘 남은 호출량 및 사용률
- 이번 달 사용량 / 775,000 무료구간
- 월 잔여 무료 호출량 및 사용률
- 최근 1시간 호출량
- 마지막 호출 시간
- 최근 오류

StockLog 자체 안전 예산:

- 15,000회: 주의 상태
- 20,000회: 일반 백그라운드 뉴스 수집 절약 모드
- 24,950회: 한도 보호
- 25,000회: 일일 호출 중지

수동 갱신/사용자 요청은 일반 백그라운드 수집보다 우선하며, 하드 보호선에 도달하면 모두 중지됩니다.

### OpenDART

- 오늘 전체 호출량
- 성공/실패
- 최근 1시간 호출량
- 백그라운드/사용자/관리자 호출 구분
- 마지막 호출
- 최근 오류

## 런타임 설정 우선순위

1. MySQL 관리자 저장값
2. 기존 `.env`
3. 미설정

OpenDART 재무 동기화, 기업개황, 주식수, 배당, 공시와 네이버 뉴스 수집 모두 같은 설정 공급자를 사용하도록 통합했습니다.

## 보안

- API Secret/API Key 평문 DB 저장 금지
- API 조회 응답에 실제 Secret/API Key 포함 금지
- 호출 로그에는 요청 URL의 비밀 query parameter를 저장하지 않음
- `JWT_SECRET`을 변경하면 기존 암호화 API 자격증명도 복호화할 수 없으므로 운영 중 임의 변경에 주의해야 합니다.

## 적용

기존 `.env`의 `DART_API_KEY`, `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`은 그대로 두어도 됩니다.
서버 기동 후 관리자 > 외부 API 관리에서 값을 입력해 저장하면 그 시점부터 MySQL 저장값이 우선 사용됩니다.
