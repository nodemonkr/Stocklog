# StockLog v3.42.0

## 관리자 페이지 재설계
- 관리자 첫 화면의 큰 통계 카드와 긴 설명을 제거하고 핵심 상태 4개만 한 줄로 압축했습니다.
- 동기화 관련 기능을 하나의 `데이터 동기화` 허브로 통합했습니다.
- 외부 API 설정/모니터링을 NAVER / OpenDART 두 개의 컴팩트 카드로 묶었습니다.
- 불필요한 공백, 중복 설명, 과도하게 큰 숫자 영역을 줄였습니다.

## 동기화 구조
- 단계별 수동 동기화
  1. 키움 시세
  2. DART 재무
  3. 키움 테마
  4. 시장 테마
  5. 종목 분류
- `전체 동기화`는 위 단계를 순서대로 한 번에 실행합니다.
- 전체 동기화 화면에서 현재 단계, 진행률, 최근 완료 시각, 다음 자동 실행 시각을 확인할 수 있습니다.
- 매일 22:00(Asia/Seoul)에 전체 동기화를 자동 실행합니다.
- 백엔드 재시작 후 남은 stale running 상태는 안전하게 종료 상태로 정리합니다.

## 분석 점수 재계산
별도 관리자 버튼을 제거했습니다.

키움 시세/기본지표와 DART 재무 데이터가 갱신될 때 이미 `Stock.score = compute_score(stock)`가 실행되므로 별도 전체 재계산은 정상 운영에서는 중복입니다. 기존 `/api/admin/sync/stocks` 엔드포인트는 구버전 호환용으로 유지합니다.

## 자동 동기화 주의
- 자동 동기화에는 관리자 계정에 저장된 키움 인증정보와 OpenDART API 키가 필요합니다.
- 뉴스는 API 호출량 보호를 위해 전체 상장종목을 22시 작업에서 일괄 수집하지 않으며 기존 캐시/중요종목/사용자 조회 기반 수집 방식을 유지합니다.
