# StockLog mobile baseline — read first

Before modifying or building this mobile app, read `V3_80_0_CHANGES.md`. It is the known-good Android native baseline and records the required Expo/EAS/API/TypeScript constraints and previously fixed failure modes. Preserve those constraints unless the current task explicitly requires changing them.

# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.
