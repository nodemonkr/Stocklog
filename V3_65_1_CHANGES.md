# StockLog v3.65.1

## 테마 사업분류 버그 수정

- v3.65.0에서 AI 사업분류 결과가 `{"items":{"종목코드":...}}` 형식일 때만 인식되던 문제를 수정했습니다.
- Gemini가 `items` 배열, 회사명 key, 숫자형 종목코드, `stock_code`/`ticker` 필드를 반환해도 정확한 StockLog 종목코드로 정규화합니다.
- 이전 버전에서 AI 분류 실패 시 공급사 테마를 `investment_theme`에 45점 신뢰도로 저장하던 로직을 제거했습니다.
- 기존 DB에 남아 있는 저신뢰도/임시 분류는 다음 AI 테마 정리 실행 시 자동으로 비웁니다.
- 스마트 분석은 신뢰도 60 미만 또는 임시 분류를 정식 사업기반 테마로 사용하지 않습니다.

## 사업기반 보완 분류

- AI가 특정 종목 결과를 누락해도 공급사 테마, 최근 뉴스, 증권사 리포트, 기존 사업정보를 교차검증하는 규칙 기반 fallback을 추가했습니다.
- 거래소 공식 업종은 매우 낮은 가중치만 사용합니다. 광범위한 `화학`, `기계` 등의 업종이 실제 투자테마를 덮어쓰지 않도록 했습니다.
- 대표적인 명백 오분류 사례인 코스맥스는 `화장품 / K-뷰티 / 화장품 ODM` 사업분류 검증 규칙을 포함합니다.
- 근거가 부족하면 임의 테마를 저장하지 않고 `검수 필요`로 남깁니다.

## 관리자 진행상황 수정

- v3.65.0에서 사업분류를 모두 순회한 뒤에도 `100 / 2,629`로 되돌아가던 완료 카운터 오류를 수정했습니다.
- 관리자 테마 정리 화면에 다음 지표를 분리 표시합니다.
  - 전체 처리
  - 대표 테마
  - 사업 검증
  - AI 분류
  - 규칙 보완
  - 대표 분류 저장
  - 검수 필요
- 완료 메시지에서도 실제 사업검증 처리수와 저장수, 검수 필요수를 따로 표시합니다.

## 스마트 분석 보호

- 낮은 신뢰도의 이전 `investment_theme`는 스마트 종목 카드, 테마 배지, 테마 필터에서 정식 분류로 취급하지 않습니다.
- 검증된 분류가 없을 때만 공급사 canonical 테마를 임시 표시합니다.
- 검증된 사업분류가 저장되면 해당 대표/보조 투자테마가 스마트 분석에 우선 적용됩니다.
