# StockLog v3.75.3

## Gbot 자동매매 429 안정화

- 자동매매 Gbot은 반복 호출 전용 `gemini-3.5-flash-lite`를 우선 사용합니다. 프리미엄 종목 분석의 `gemini-3.6-flash`와 호출량을 분리합니다.
- Gemini가 HTTP 429를 반환하면 다른 사용 가능한 Flash 모델을 순차 확인합니다.
- 모든 모델이 요청 제한 상태이면 키움 주문을 절대 생성하지 않고 해당 회차를 안전하게 건너뜁니다.
- Google 응답의 `Retry-After` 또는 `retryDelay`를 읽어 다음 자동 판단 시점을 늦춥니다.
- 429 원문 URL/HTTP 예외를 사용자 화면에 노출하지 않고 `Gbot 호출 한도 대기` 상태와 다음 판단 시각을 표시합니다.
- StockLog 자체 Gemini 백그라운드 안전 한도 문구를 실제 동작과 일치하도록 수정했습니다.
- 기존 v3.75.2의 확신도 기반 매수 수량 계산, 최소 1주 보정, 자금 안전 한도 및 자동매매 UI 개편을 그대로 포함합니다.
