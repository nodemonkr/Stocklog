# StockLog v3.61.2

## 전체 동기화 안정성 수정

- `full_market_sync_state.provider_status_json`, `failures_json`을 시작 시 `MEDIUMTEXT`로 자동 확장합니다.
- DB ALTER 권한이 없거나 기존 TEXT 스키마를 그대로 쓰는 경우에도 실패 상세를 자동 축약해 약 64KB TEXT 한도를 넘지 않도록 방어합니다.
- 시장 테마 실패 상세는 최근 항목만 보관하고 개별 오류 문자열도 길이를 제한해 상태 로그가 기하급수적으로 커지지 않게 했습니다.
- 동기화 예외/취소 처리 시 항상 `Session.rollback()` 후 상태 행을 다시 조회하여 `PendingRollbackError` 연쇄 오류를 차단합니다.

## 레거시 테마 DB 스키마 호환

- 구버전 `themes.theme_name` 컬럼이 `NOT NULL`로 남아 있는 DB를 자동 감지합니다.
- 기존 `theme_name` 값을 현재 `name`으로 보강하고, 레거시 `theme_name`을 nullable로 변경해 신규 테마 INSERT가 실패하지 않도록 수정했습니다.

## 이번 로그에서 확인된 중단 위치

- 시세 · 재무: 완료
- 키움 테마: 완료
- 시장 테마 목록: 266개 조회 완료
- 시장 테마 구성종목: 102 / 266번째까지 시도 후 중단
- 당시 성공 0 / 실패 102, 연결 종목 0개
- 종목 분류: 아직 시작 전

`37.9624%`는 전체 동기화 퍼센트가 아니라 시장 테마 266개 중 102개를 시도한 해당 세부 단계의 진행률입니다.
