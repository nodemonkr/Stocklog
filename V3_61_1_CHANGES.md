# StockLog v3.61.1

## 전체 동기화 안정화

- OpenDART 공시에 특정 재무 항목이 없는 경우 `financial_quarters`의 핵심 재무값을 `NULL`로 저장할 수 있도록 ORM 및 기존 MySQL 스키마 자동 보정 로직을 추가했습니다.
- `NULL`을 임의의 `0`으로 바꾸지 않아 성장률, 영업이익률, 밸류에이션 분석 왜곡을 방지합니다.
- 재무 저장 중 flush/commit 오류가 발생하면 즉시 `Session.rollback()` 후 원래 오류를 다시 전달하도록 수정했습니다.
- 시세/재무 전체 수집 루프의 개별 종목 실패 처리와 최상위 작업 실패/중지 처리에도 rollback 방어를 추가해 `PendingRollbackError` 연쇄 실패를 막았습니다.
- 종목 상세에서 OpenDART 재무 보강 실패 시에도 세션을 복구한 뒤 경고만 남기도록 보강했습니다.

## 관리자 동기화 UI

- 전체 동기화 진행률을 하위 작업의 실제 진행률과 합산해 더 촘촘하게 표시합니다.
- 실시간 동기화 패널에 현재 처리 종목명/코드, 시장, 단계 메시지, 처리 건수, 단계 진행률, 성공/실패 수, 예상 잔여시간을 표시합니다.
- 단계 트랙에서 실행 중인 단계의 실제 진행률을 함께 표시합니다.
- 기존 관리자 상태 폴링 주기(약 1.2초)를 활용해 별도 새로고침 없이 진행상황이 갱신됩니다.
