# StockLog v3.68.4

## 종목명 무결성 / 공식 사명 변경 대응

- KRX KIND 회사명을 기존 종목명에 무조건 덮어쓰던 v3.68.3 로직을 제거했습니다.
- KIND HTML은 `회사명`/`종목코드` 헤더를 명시적으로 확인한 경우에만 종목명 변경 권한을 갖습니다.
- 위치 추정 fallback 파싱 결과는 기존 종목명을 절대 덮어쓰지 못합니다.
- 공식명이 변경되면 이전 이름을 `name_aliases_json`에 보존합니다.
- 현재 공식명 출처(`name_source`), 검증시각, 변경시각을 DB에 저장합니다.
- 종목 검색/자동완성/스마트 검색은 현재 이름뿐 아니라 이전 이름도 검색합니다.
- 스마트 목록/상세에서는 이름 변경 이력이 있으면 `구 ...`를 함께 표시합니다.
- v3.68.3에서 이미 유실된 030520의 이전 이름 `한글과컴퓨터`를 검색 alias로 복구합니다. 현재 공식명 `한컴`은 그대로 유지합니다.
- Universe 상태에 공식명 변경 수와 검증되지 않은 이름 덮어쓰기 차단 수를 기록합니다.
