# StockLog v3.76.12 변경사항

## 프로필 중심 계정 기능 재배치
- 왼쪽 메뉴의 `거래 연동`, `포트폴리오`, `투자 성향 분석` 항목을 제거했습니다.
- 프로필 화면에 `내 투자 · 거래 관리` 영역을 추가했습니다.
- 프로필에서 아래 기능으로 바로 이동할 수 있습니다.
  - 거래 연동
  - 포트폴리오
  - 투자 성향 분석 / 재검사

## 사이드바 정보 구조 정리
- `WORKSPACE` 그룹명을 `종목 분석`으로 변경했습니다.
- `스마트 분석`, `인기테마 분석`, `수급 분석`을 `종목 분석` 그룹으로 묶었습니다.
- `강세 테마` 메뉴명을 `인기테마 분석`으로 변경했습니다.
- 모의투자 그룹에는 `모의투자(수동)`, `모의투자(자동)`만 표시합니다.
- 관리자 메뉴는 일반 메뉴 흐름과 분리해 사이드바 최하단에 고정되도록 했습니다.
- 모바일 하단 메뉴에서도 관리자 메뉴가 마지막 순서로 유지됩니다.

## 프로필 테마 설정
- 프로필에 `밝게`, `어둡게` 화면 테마 선택 기능을 추가했습니다.
- 선택한 테마는 `localStorage`에 저장되어 같은 브라우저에서 유지됩니다.
- 어두운 테마에서 주요 페이지 배경, 패널, 입력창, 표, 프로필 카드와 상세 화면이 함께 어두운 색상으로 전환됩니다.

## 로그인 정보 카드 개선
- 사이드바 로그인 카드를 프로필 중심 구조로 재디자인했습니다.
- 사용자명, 회원 등급, AI 사용량, 계정 관리, 로그아웃을 한 카드 안에서 더 단순하게 확인할 수 있게 했습니다.
- 불필요한 장식과 복잡한 정보 배치를 줄였습니다.

## 배포 호환
- 소스 코드에는 v3.76.12 UI를 직접 반영했습니다.
- 외부 npm 설치가 불가능한 환경에서도 바로 사용할 수 있도록 기존 검증 production bundle 위에 v3.76.12 UI bridge를 포함한 `frontend/dist`를 함께 제공합니다.
- `restart-all.sh`는 npm 재빌드가 실패하더라도 동봉된 production dist를 유지하도록 되어 있습니다.

## 검증
- `frontend/src/App.jsx` JSX 파싱 검사 통과
- `frontend/src/style.css` CSS 파싱 검사 통과
- production patch JS `node --check` 통과
- production bundle JS `node --check` 통과
- production CSS 파싱 검사 통과
- backend Python `compileall` 통과
- 전체 shell script `bash -n` 검사 통과
