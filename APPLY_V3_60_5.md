# StockLog v3.60.5 적용 방법

## 권장: 핫픽스 적용
프로젝트 루트 `/home/conda/Stocklog`에서 핫픽스 압축의 아래 파일을 같은 경로에 덮어씁니다.

- `frontend/src/App.jsx`
- `frontend/src/style.css`
- `backend/app/main.py`

이후:

```bash
cd /home/conda/Stocklog
./restart-all.sh
```

브라우저에서는 `Ctrl + Shift + R`로 강력 새로고침합니다.

## 확인 포인트
1. 신규 일반회원 가입 화면에서 아이디를 입력하면 자동으로 사용 가능 여부가 표시됩니다.
2. 각 필드는 정상일 때 초록색, 오류일 때 빨간색으로 표시됩니다.
3. 필수 항목/약관이 모두 정상일 때만 `다음` 버튼이 활성화됩니다.
4. 회원가입 시 완료한 투자 성향 결과가 있는 계정은 로그인 후 `투자 성향 분석` 메뉴에서 바로 `나의 투자 성향` 결과가 표시됩니다.
5. 문항 화면은 결과 페이지의 `다시 테스트`를 눌렀을 때만 시작합니다.
