# StockLog v3.54 변경사항

## 1. 프리미엄 이상 보유종목 AI 모멘텀 자동 분석
- 새 회원 기능 키 `portfolio_ai_momentum` 추가.
- 기본 권한: NORMAL 차단 / PREMIUM·EVENT·ADMIN 허용.
- 모의투자 보유종목이 확인되면 사용자가 별도 버튼을 누르지 않아도 백그라운드에서 모멘텀 분석을 준비한다.
- 분석 입력은 StockLog DB의 실제 일봉에서 계산한 1·5·20·60일 수익률, 20/60일 이동평균 위치, 20일 평균 대비 거래량, 52주 고점 거리로 제한한다.
- Ollama는 짧은 한국어 모멘텀 코멘트만 생성하며 매수·매도 지시는 하지 않는다.
- AI 서버가 느리거나 실패하면 동일한 입력 데이터로 계산한 deterministic fallback을 표시한다.
- 결과는 `ai_stock_analysis`의 `portfolio_momentum` 모드로 종목 단위 공유 캐시한다. 개인 보유수량/평단은 AI 컨텍스트에 넣지 않아 회원간 개인정보가 섞이지 않는다.
- 기본 캐시 TTL 4시간(`PORTFOLIO_AI_MOMENTUM_CACHE_HOURS`). 동일 종목을 여러 프리미엄 회원이 보유해도 같은 분석을 재사용한다.
- 자동 분석은 프리미엄 기능 자체에 포함되며 수동 AI 일일 사용횟수를 차감하지 않는다.
- 보유종목 목록에 AI 모멘텀 배지, 선택 종목 상세에 상태/신뢰도/요약/체크포인트/분석시각을 표시한다.
- 일반회원에는 프리미엄 기능 안내 카드만 표시한다.

## 2. 등급별 자동 새로고침 정책
- `membership_refresh_policies` 테이블 추가.
- 관리자 페이지에서 NORMAL/PREMIUM/EVENT/ADMIN 각각 아래 값을 별도로 설정한다.
  - 모의투자 자동 갱신 초
  - 강세 테마 자동 갱신 초
- 0초는 자동 갱신 해제, 1~9초는 과도한 호출 방지를 위해 저장하지 않으며 10~3600초를 지원한다.
- 기본값:
  - NORMAL: 모의투자 45초 / 테마 120초
  - PREMIUM: 30초 / 60초
  - EVENT: 20초 / 45초
  - ADMIN: 15초 / 30초
- 모의투자 현재가는 기존 WebSocket 실시간 수신을 유지한다. 정책 주기는 계좌 원장과 주문가능금액의 백그라운드 갱신에만 적용한다.
- 계좌 원장과 주문가능금액은 같은 순간에 키움으로 몰리지 않도록 약 1.3초 간격으로 순차 갱신한다.
- 강세 테마 자동 갱신은 브라우저가 visible 상태일 때만 실행하며, 백엔드의 기존 테마 캐시/fallback 정책을 그대로 사용한다.

## 3. 보유종목 매수/매도 버튼 통일
- 두 버튼을 동일 컴포넌트 스타일(`holding-detail-trade-button`)로 통일.
- 크기, 라운드, 아이콘, 타이포, hover 효과는 동일하고 buy/sell 색상만 구분한다.

## 4. 호환성 및 안전
- 기존 NORMAL/PREMIUM/EVENT/ADMIN 등급 체계와 기능 정책 UI를 유지한다.
- 기존 소셜 로그인, 스마트 분석, 수급 분석, 키움 호출 큐/Rate Limit 보호, 테마 fallback, 모바일 반응형, production dist 교체 방식은 유지한다.
- DB 신규 테이블은 SQLAlchemy `create_all()`로 기존 DB를 삭제하지 않고 생성된다.
