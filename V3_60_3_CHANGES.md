# StockLog v3.60.3

## 일반 회원 로그인/회원가입 복구
- 홈페이지에 일반 아이디/비밀번호 로그인을 다시 활성화했습니다.
- 신규 일반 회원가입 API(`/api/auth/register`)를 다시 활성화했습니다.
- 신규 회원은 이름, 성별, 생년월일, 휴대폰 번호, 아이디, 비밀번호를 입력한 뒤 기존 30문항 투자성향 검사를 완료하면 가입됩니다.
- 일반 회원 로그인은 `/api/auth/login`, 관리자 전용 진입(`/?admin=1`)은 `/api/auth/admin-login`을 그대로 사용합니다.

## 생년월일
- 생년월일은 연/월/일 드롭다운으로 선택합니다.
- 예: `1995년 / 01월 / 04일`.
- `users.birth_date` DATE 컬럼을 자동 마이그레이션하고, 호환성을 위해 `birth_year`도 함께 저장합니다.
- 일반 회원 나이는 전체 생년월일 기준으로 계산합니다.

## SNS 로그인
- Google/Naver/Kakao OAuth 기능은 유지했습니다.
- 관리자에서 `로그인 화면에 노출`을 끄면 홈페이지 로그인/회원가입 화면에서 SNS 영역 자체를 숨깁니다.
- 다시 노출을 켜면 기존 OAuth 흐름을 그대로 사용할 수 있습니다.

## UI 정리
- 로그인/회원가입 화면의 불필요한 설명 문구를 제거했습니다.
- 필수 입력값, 오류 메시지, 약관 동의, 투자성향 단계만 남겼습니다.
