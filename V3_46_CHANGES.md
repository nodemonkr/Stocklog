# StockLog v3.46 변경사항

## 1. 사이드바 계정 접속 정보 UI
- 좌측 상단 사용자 영역을 현대적인 계정 카드 형태로 재디자인했습니다.
- 표시 이름, 아이디, 접속 상태, 계정 유형(일반/테스트/관리자), 오늘 AI 잔여 횟수를 한 영역에서 확인할 수 있습니다.
- 중간 폭/모바일에서는 기존 compact navigation 규칙을 유지합니다.

## 2. 스마트 분석 추천 목록 개편
- 목록의 `AI 의견` 열을 제거했습니다.
- 기존의 작은 9열 테이블을 종목 / 현재가 / 밸류에이션(PER·PBR) / 수익성(ROE) / 성장성 / 추천 이유 / 추천점수 구조로 단순화했습니다.
- PER/PBR, ROE, 매출성장률을 각각 시각적인 metric card로 구분했습니다.
- 추천 이유는 최대 4개의 읽기 쉬운 배지 형태로 표시합니다.
- 추천점수는 큰 숫자, 100점 기준 progress bar, 등급 문구를 함께 표시합니다.
- 태블릿/모바일에서는 가로 스크롤 대신 카드형 레이아웃으로 전환됩니다.

## 3. AI 공용 캐시 + 사용자별 열람권한
- AI 결과 본문은 기존처럼 `ai_stock_analysis`에 종목/모드별 공용 캐시로 한 번만 저장합니다.
- 신규 `ai_analysis_access` 테이블은 `user_id + stock_code + mode`의 작은 권한 데이터만 저장합니다.
- 일반회원은 본인이 AI 1회를 사용해 열람권한을 얻은 결과만 확인할 수 있습니다.
- 다른 사용자가 이미 생성한 유효한 AI 캐시가 있으면 재추론하지 않고 즉시 재사용하므로 AI 서버 리소스를 절약합니다.
- 같은 계정이 이미 열어본 캐시를 다시 보는 것은 추가 사용횟수를 차감하지 않습니다.
- `다시 분석(force)` 요청은 일반회원에게 새로운 AI 사용 1회로 계산됩니다.
- 동일 종목/모드 분석이 이미 진행 중이면 사용자별 중복 추론을 시작하지 않고 기존 공용 작업에 연결합니다.
- 테스트계정과 관리자계정은 기존처럼 AI 열람/사용이 무제한입니다.

## 4. 관리자 계정 목록 확장성 개선
- `/api/admin/users`를 서버 페이지네이션 방식으로 변경했습니다.
- 기본 20명 단위로만 DB에서 조회합니다.
- 아이디/표시 이름 서버 검색, 일반회원/테스트/관리자 유형 필터를 지원합니다.
- 가입자 1만 명 이상에서도 모든 계정을 한 번에 전송하거나 렌더링하지 않습니다.
- 현재 페이지의 AI 사용량은 사용자별 N회 쿼리가 아니라 페이지 전체를 한 번의 사용량 쿼리로 가져옵니다.

## 5. 주요 시장지표
- 스마트 분석 상단 수동 새로고침 버튼을 제거했습니다.
- 기존 30초 자동 새로고침은 유지합니다.
- 지표 영역을 반응형 grid로 변경해 옆으로 스크롤하지 않고 모든 지표를 확인할 수 있습니다.

## DB 반영
백엔드 시작 시 SQLAlchemy `Base.metadata.create_all()`에 의해 아래 신규 테이블이 자동 생성됩니다.

- `ai_analysis_access`

기존 사용자/AI 캐시/일일 AI 사용량 테이블은 유지됩니다.
