# StockLog v3.75.15 변경사항

## 키움 모의계좌 원장 정합성
- `kt00004`의 `prsm_dpst_aset_amt`를 총자산의 최우선 기준으로 사용합니다.
- `tot_est_amt`는 총자산이 아니라 유가잔고 평가액으로 분리했습니다.
- 동일 영업일 매수 후 예수금과 보유평가액을 단순 합산해 총자산을 만드는 로직을 제거했습니다.
- 종목별 `evlt_amt / pl_amt / pl_rt`를 키움 비용반영 평가손익으로 보존하고, `현재가 × 수량`은 별도의 `market_value`로 분리했습니다.
- 상단 금일손익은 키움 `tdy_lspft / tdy_lspft_rt`가 있을 때 이를 우선 사용합니다.

## 시장가 자동매매 체결가 복구
- 시장가 주문에서 주문가격(0원)을 실제 체결가보다 먼저 읽던 문제를 수정했습니다.
- `ka10076.cntr_pric` 및 체결단가 필드를 주문가격보다 우선합니다.
- 이미 체결 완료된 과거 Gbot 주문도 broker 주문번호가 일치하면 실제 체결가로 자동 보정합니다.
- 현재 보유수량 전체가 Gbot 귀속인 종목은 키움 계좌 평균단가/매입금액으로 자동매매 원장을 self-heal 합니다.

## 자동운용 평가액
- 자동운용 평가액/손익도 포트폴리오와 동일한 키움 비용반영 평가액을 Gbot 귀속 수량 비율만큼 반영합니다.
- 수동+Gbot 혼합 보유는 Gbot 수량 비율만 자동운용 금액에 포함합니다.

## UI
- 포트폴리오 상단 `평가금액`을 `유가잔고 평가액`으로 명확히 표기했습니다.
- 종목별 평가금액에는 `키움 비용반영 평가액` 기준을 표시합니다.
- 금일 종목 손익은 `금일 변동손익`, 계좌 상단은 `금일 투자손익`으로 의미를 분리했습니다.
