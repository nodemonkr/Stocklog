# StockLog v3.64.0 Changes

## 스마트 분석: AI 점수와 개인 성향 점수 완전 분리

이번 버전의 핵심 목표는 사용자가 "왜 이 종목이 추천되는지"와 "좋은 종목인지 / 나에게 맞는 종목인지"를 서로 구분해서 이해할 수 있게 만드는 것입니다.

### 1. StockLog AI 추천 점수

스마트 분석의 숫자 추천 점수를 설명 가능한 점수 엔진으로 통합했습니다.

- 재무제표 25%
- 밸류에이션 20%
- 최근 주가 흐름 15%
- 외국인/기관 수급 15%
- 최근 6개월 뉴스·증권사 리포트 15%
- 안정성 10%

데이터가 아직 동기화되지 않은 영역은 0점으로 벌점을 주지 않고 분석 커버리지에서 제외한 뒤, 실제 확보된 데이터의 가중치를 재조정합니다. 따라서 관리자 동기화 범위에 따라 점수 신뢰 범위를 함께 확인할 수 있습니다.

숫자 점수는 LLM이 임의로 생성하지 않습니다. Gemini/Ollama AI Bot은 별도의 심층 해설 역할을 담당합니다. 이렇게 분리해 동일 데이터에 대한 점수 일관성과 설명 가능성을 확보했습니다.

### 2. 내 투자성향 적합 점수

기존에는 성향 점수와 기본 추천 점수가 하나의 숫자로 섞여 보여 두 값의 의미를 알기 어려웠습니다.

v3.64.0부터는:

- `StockLog AI 추천 점수`: 종목 자체의 데이터 평가
- `내 투자성향 적합 점수`: 같은 종목이 사용자의 투자 DNA와 얼마나 맞는지 평가

를 독립적으로 계산하고 동시에 표시합니다.

보유기간, 위험선호, 성장/가치 선호, 수익실현 성향, 집중/분산 선호를 각 분석 항목의 중요도와 연결합니다.

### 3. 스마트 종목 목록 UI 개편

각 종목 행에서 다음 정보를 동시에 확인할 수 있습니다.

- 현재가 / 등락률
- PER / PBR / ROE / 매출 성장
- StockLog AI 추천 점수 + 판단 구간
- 내 투자성향 적합 점수 + 적합 구간
- 현재 분석 데이터 커버리지
- 추천 핵심 요약
- `점수 상세`
- `종목 상세`

성향 검사를 완료하지 않은 사용자도 StockLog AI 점수는 정상적으로 볼 수 있으며 개인 성향 영역만 검사 필요 상태로 표시합니다.

### 4. 점수 상세 설명창 추가

`점수 상세` 버튼으로 종목 전체 상세페이지를 열기 전에 점수의 생성 근거를 독립적으로 확인할 수 있습니다.

항목별로 다음을 비교합니다.

1. 재무제표 기준 분석
2. 밸류에이션 기준 분석
3. 주가 흐름 기준 분석
4. 수급 기준 분석
5. 뉴스·리포트 기준 분석
6. 안정성 기준 분석

각 항목에서:

- StockLog AI 항목 점수
- 내 성향 적합도
- StockLog 판단 설명
- 내 성향과 비교한 설명
- 실제 사용된 지표/데이터
- 데이터 출처

를 함께 표시합니다.

### 5. 신규 API

`GET /api/smart/stocks/{code}/score-detail`

스마트 점수 상세 비교를 반환합니다. 외부 API를 새로 호출하지 않고 이미 관리자 동기화로 저장된 StockLog DB 데이터를 사용합니다.

### 6. 성능 / 구조

- 스마트 목록에서 뉴스/리포트/수급을 종목별 N+1 쿼리로 조회하지 않고 후보 종목을 대상으로 일괄 조회합니다.
- 전체 상장종목의 무거운 감성/수급 데이터를 매 요청마다 계산하지 않고 1차 후보군을 만든 뒤 상세 점수를 계산합니다.
- 점수 계산 로직을 `backend/app/smart_scoring.py`로 분리했습니다.
- 점수 엔진은 FastAPI/SQLAlchemy와 독립된 순수 계산 모듈이라 단위 테스트가 가능합니다.

### 7. 테스트

`backend/tests/test_smart_scoring.py`를 추가했습니다.

- AI 점수와 성향 점수가 독립적으로 계산되는지
- 성향 미검사 상태에서도 AI 점수가 유지되는지
- 수급/뉴스 데이터가 없을 때 0점 처리 대신 커버리지가 낮아지는지
- 6개 분석 영역이 모두 설명 데이터와 함께 반환되는지

를 검증합니다.
