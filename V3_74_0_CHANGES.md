# StockLog v3.74.0

## 관리자 데이터 동기화 개편

- 전체 동기화를 8개 항목으로 분리: 키움 시세, DART 재무, 키움 테마, 시장 테마, 종목 분류, 표준 테마, 수급 데이터, 스마트 분석 점수
- 수동 전체 동기화 실행 전 체크박스 선택 모달 추가
- 자동 전체 동기화에도 실행 항목 선택 및 저장 기능 추가
- 표준 테마 재구축을 전체/자동 동기화의 정식 단계로 포함
- 전체 실행 상태는 과거 개별 작업 시간이 아니라 해당 실행의 단계 스냅샷을 표시
- 선택하지 않은 단계는 `이번 실행 제외`, 앞 단계 오류로 진행하지 못한 단계는 `미실행`으로 명확히 구분
- 각 단계에 이번 실행 시작/완료 시각을 기록
- 일부 종목 실패가 발생한 단계는 가능한 경우 전체 작업을 중단하지 않고 `확인 필요`로 처리 후 다음 단계 계속 진행
- 원시 실패/스킵/재시도 카운터를 메인 화면에서 제거하고 단계 중심 상태 UI로 단순화
- 개별 항목만 실행할 때도 통합 실행 엔진을 사용해 상태 기록을 일관되게 유지
- 자동 스케줄의 선택 항목을 DB에 영구 저장하며 기존 DB는 시작 시 자동 마이그레이션
- 동기화 진행 화면을 현재 단계, 처리 건수, 단계 진행률, 예상 잔여시간 중심으로 간소화

## 호환성

- 기존 스케줄 설정은 최초 적용 시 8개 항목 전체 선택 상태로 자동 승계됩니다.
- 기존 개별 동기화 API는 유지됩니다.
- v3.73.2의 Gbot JSON 안정화/Gemini 3.6 Flash 수정 내용도 그대로 포함합니다.
