# StockLog v3.75.0

## 모의투자(자동) / StockLog Gbot 자동 운용

- 기존 `모의투자` 메뉴를 `모의투자(수동)`으로 명확히 분리했습니다.
- `모의투자(자동)` 페이지를 신규 추가했습니다.
- 자동 운용은 **키움 모의투자 계좌에만** 주문하며 실계좌 사용은 허용하지 않습니다.
- StockLog Gbot이 동기화된 종목 데이터(스마트 점수, 재무/밸류, 모멘텀, 변동성, 거래량, 수급, 테마/카테고리)를 한 번에 비교해 `매수/매도/관망`을 판단합니다.
- 자동매매 설정: 시장, 카테고리, 전체/선택 테마, 주가, 시가총액, 평균 거래량, 최소 StockLog 점수, Gbot 최소 확신도, 후보 수, 자동 운용 총한도, 종목당 한도, 최대 보유종목, 일 주문횟수, 회차별 신규매수 개수, 최소 현금 보유율, 거래시간/주기, 손절/익절 기준을 제공합니다.
- 기본값에서는 자동매매가 체결해 소유한 수량만 자동 매도합니다. 수동 보유수량 매도 허용은 별도 위험 설정으로 분리했습니다.
- Gbot 제안보다 서버의 자금/현금/종목/주문횟수 안전 한도가 우선하며 차단 이유를 이력에 기록합니다.
- 서버 재시작 후에도 `거래 시작` 상태는 DB에 유지되고 자동 판단 watcher가 다시 이어집니다.
- 같은 종목 미체결 자동주문이 있을 때 중복 주문하지 않습니다.
- 미체결 매수 금액을 다음 매수의 가용 자금/자동운용 한도에서 예약 금액으로 차감합니다.
- 자동 주문 체결을 키움 주문번호로 재확인하여 자동매매 전용 보유수량/평균단가를 갱신합니다.
- 각 판단/주문에 종목, 시간, 수량, 판단가격/체결가, 금액, 확신도, 상세 이유, 핵심 근거, 위험요인, 재평가/청산 계획, 안전장치 차단 사유, 사용 Gbot 모델, 키움 주문번호를 저장합니다.
- 거래 이력과 전체 판단 로그(관망/차단 포함)를 분리해서 볼 수 있습니다.

## 기존 기능 보호

- 수동 모의투자 주문 API와 원장은 그대로 유지하고 자동매매 전용 설정/포지션/판단 테이블을 별도로 추가했습니다.
- 관리자 v3.74.0 선택형 전체 동기화 기능과 Gbot 프리미엄 분석 수정사항을 그대로 포함합니다.
- 회원 삭제 시 자동매매 전용 사용자 데이터도 함께 정리합니다.
