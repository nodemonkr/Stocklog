# StockLog v3.62.1 Changes

## AI 테마 정리 안정화
- 관리자 AI 테마 정리를 백그라운드 작업으로 변경했습니다. 버튼 클릭 즉시 작업이 시작되고 진행률, 처리 건수, 현재 비교 테마, AI 묶음, 대표 테마 수, 묶인 이름 수를 실시간 표시합니다.
- 작업 상태를 `full_market_sync_state`에 저장해 관리자 화면 새로고침 후에도 진행 상태를 확인할 수 있습니다.
- AI 호출을 40개 단위로 나눠 긴 요청의 무응답처럼 보이는 문제를 제거하고, 각 묶음은 1회 자동 재시도합니다.
- 가능하면 `themes.canonical_name` 컬럼을 자동 추가해 공급사 원본명과 스마트 분석 대표명을 분리합니다. 이후 키움/인포스탁 동기화를 다시 실행해도 AI 대표명이 덮어써지지 않습니다.
- ALTER 권한이 없는 DB도 동작하도록 호환 저장 경로를 유지합니다.

## 스마트 분석 테마 연결 수정
- `/api/smart/filter-options`의 MySQL `DISTINCT + ORDER BY` 호환성 문제를 제거했습니다. 해당 오류가 발생하면 기존 프론트가 빈 배열로 처리해 테마 필터가 비어 보일 수 있었습니다.
- 스마트 분석 테마 배지와 필터는 `canonical_name`이 있으면 대표명을, 없으면 공급사 원본명을 사용합니다.
- 서로 다른 theme_code가 같은 대표 테마로 묶여도 종목 카드에서는 하나의 대표 테마로 중복 제거됩니다.
- 대표 테마 필터 선택 시 해당 대표명에 연결된 모든 공급사 theme_code의 종목을 함께 조회합니다.
- 테마 정리/데이터 갱신 이벤트 후 필터 옵션을 다시 불러옵니다.
