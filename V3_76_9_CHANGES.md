# StockLog v3.76.9 변경사항

## 표준 테마 분류 고도화
- 표준 테마 재구축 전에 기존 성공 분류를 스냅샷으로 보존하여 다음 실행의 보조 근거로 재활용합니다.
- 공급사 테마, 기존 표준 테마, 기존 투자테마, DART 공식 업종, StockLog 사업분류, 뉴스, 증권사 리포트, 종목명/이전 종목명을 함께 점수화합니다.
- 의미가 명확한 DART 업종은 낮은 신뢰도의 `업종 보조분류`로 사용할 수 있습니다.
- `화학`처럼 범위가 지나치게 넓은 업종은 기존 원칙대로 단독으로 투자테마를 강제하지 않습니다.
- 표준 테마 분류 대상은 일부 근거 보유 종목이 아니라 전체 `is_analysis_eligible` 종목으로 통일했습니다.

## 무테마와 오류의 분리
- 충분한 근거가 없는 종목은 `정상 무테마`로 처리하며 동기화 실패로 집계하지 않습니다.
- `확인 필요(partial)`는 코드 예외/DB 저장 실패 등 실제 처리 오류가 있을 때만 표시합니다.
- 통합 동기화의 표준 테마 단계는 `미분류/무테마` 개수 때문에 경고 상태로 바뀌지 않습니다.
- `no_theme`, `strong_classified`, `fallback_classified`, `errors`, `classification_coverage` 통계를 저장합니다.
- 무테마 샘플 종목을 provider status에 최대 12개 남겨 추후 분류 사전 확장에 활용할 수 있습니다.

## 관리자 UI
- 최근 표준 테마 분류 커버리지를 `강한 근거 / 업종 보조 / 무테마 / 실제 오류`로 분리해 표시합니다.
- 무테마가 남아도 실제 오류가 0이면 `완료`로 표시합니다.
