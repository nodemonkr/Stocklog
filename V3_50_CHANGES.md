# StockLog v3.50 변경사항

## 전체 모바일 반응형 UI/UX

- 320px 소형 스마트폰부터 태블릿까지 전체 페이지 반응형 규칙을 재정비했습니다.
- 모바일 전용 상단바를 추가했습니다.
  - StockLog 로고
  - 현재 사용자 프로필 바로가기
  - 로그아웃 버튼
- 모바일 하단 메뉴는 짧은 라벨(분석/테마/모의투자/키움/성향/관리)을 사용해 관리자 계정에서도 메뉴가 겹치지 않도록 했습니다.
- Safe Area(iPhone 노치/홈 인디케이터)를 고려해 상단/하단 여백을 적용했습니다.
- 모바일 입력 요소를 16px 기준으로 조정해 iOS 입력 시 화면이 자동 확대되는 현상을 줄였습니다.
- 주요 버튼/탭의 터치 영역을 확대했습니다.

## 로그인 / 회원가입

- 모바일에서는 브랜드 좌측 패널을 제거하고 폼에 집중하는 단일 컬럼으로 표시합니다.
- 일반/카카오/네이버 로그인 버튼과 회원가입 단계가 화면 너비를 넘지 않도록 정리했습니다.
- 투자성향 30문항 선택지와 이전/다음 버튼을 모바일 터치에 맞게 확대했습니다.

## 스마트 분석

- 주요 시장지표를 모바일 2열 카드로 배치하고 초소형 화면에서는 1열로 변경합니다.
- 검색창을 전체 너비로 표시합니다.
- 추천 종목 목록은 모바일 카드형으로 표시해 페이지 전체 가로 스크롤을 제거했습니다.
- 추천 이유, 지표, 점수의 글자/간격을 모바일에서 읽기 쉽게 조정했습니다.
- 페이지네이션을 모바일 세로 흐름으로 정리했습니다.

## 강세 테마

- 테마/종목 패널을 단일 컬럼으로 전환합니다.
- 모바일에서는 목록 높이를 고정하지 않아 불필요한 이중 스크롤을 줄였습니다.
- 종목 행과 테마 선택 행의 터치 높이를 확대했습니다.

## 모의투자 / 주문

- 보유종목 목록을 모바일 카드형으로 유지하고 매도 버튼 터치 영역을 확대했습니다.
- 보유종목 상세, 투자자 수급, 차트 영역을 모바일 폭에 맞게 재배치했습니다.
- 주문창을 스마트폰에서 100vw / 100dvh 전체화면으로 표시합니다.
- 주문 탭, 주문 이력 탭은 필요한 경우 내부 가로 스크롤이 가능한 터치 탭으로 처리합니다.
- 주문 입력/호가/예약주문 영역은 1열 배치로 전환합니다.

## 종목 상세

- 상세창을 스마트폰에서 전체 화면 시트로 표시합니다.
- 닫기 버튼에 Safe Area를 적용했습니다.
- 차트 높이를 모바일 화면에 맞게 조정하고 ResizeObserver 기반 차트 리사이즈는 그대로 유지합니다.
- 재무/리포트처럼 너비가 반드시 필요한 표만 해당 카드 내부에서 가로 스크롤하도록 유지합니다.

## 키움 설정 / 투자성향 / 프로필

- 설정 카드, 저장 상태, 가이드 영역을 모바일 단일 컬럼으로 정리했습니다.
- 투자성향 결과/검사 카드와 선택 버튼을 모바일 터치에 맞게 조정했습니다.
- 계정 프로필 카드와 개인 설정 영역을 1열로 전환했습니다.

## 관리자

- 계정 목록을 모바일 카드 형태로 표시합니다.
- 계정 검색/필터는 전체 너비와 터치 스크롤 방식으로 변경했습니다.
- 동기화 단계, 소셜 로그인, 외부 API 설정을 모바일 단일 컬럼으로 변경했습니다.
- 저장/테스트/삭제 작업 버튼을 모바일에서 큰 버튼으로 표시합니다.

## 배포

- 버전을 v3.50으로 갱신했습니다.
- `restart-all.sh`의 production build를 `dist.next`에 먼저 생성한 후 성공한 경우에만 기존 `dist`와 교체하도록 강화했습니다.
- 빌드 실패 시 Caddy가 서비스 중인 기존 정상 `dist`가 유지됩니다.
- 프로젝트 기준 경로는 `/home/conda/Stocklog`입니다.
