# StockLog v3.76.13

## 자동 전체 동기화 안정화
- 자동 스케줄러가 `HH:MM`이 정확히 일치하는 1분에만 실행되던 구조를 제거했습니다.
- 등록한 회차가 다른 동기화 작업 또는 이벤트 루프 지연 때문에 제시간에 시작하지 못해도 미실행 회차로 유지합니다.
- 현재 동기화가 끝나는 즉시 가장 최근의 미실행 회차를 자동 보충 실행합니다.
- 여러 회차가 오래 밀렸을 때 과거 회차를 연속 실행하지 않고 가장 최근 회차 1건만 따라잡도록 했습니다.
- 실제 자동 동기화가 시작될 때만 `last_auto_slot`을 기록하여 시작 전에 회차가 소진되는 문제를 막았습니다.
- 활성 관리자/키움 거래 연동이 없어 자동 실행하지 못한 경우 같은 회차를 이후 다시 시도합니다.
- 스케줄 감시 간격을 30초에서 15초로 줄였습니다.
- 스케줄 저장 직후 이미 지나간 시간을 즉시 실행하지 않도록 저장 시각 이후 회차부터 적용합니다.
- 자동 실행 대기/시작 상태를 API 응답에 노출해 관리자 화면에서 확인할 수 있도록 했습니다.

## 관리자 자동 동기화 UI
- 실행 횟수 영역을 독립 설정 카드로 정리했습니다.
- 소스 UI에서는 하루 1~6회를 버튼 방식으로 선택하도록 변경했습니다.
- 1회차, 2회차 등 실행 시간을 각각 정렬된 입력 카드로 표시합니다.
- 수급 범위와 실행 시간 영역의 높이/간격/반응형 레이아웃을 정리했습니다.
- 기존 사전 빌드된 운영용 dist에도 v3.76.13 호환 CSS/JS 패치를 포함했습니다.
