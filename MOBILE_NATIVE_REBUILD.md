# StockLog Native Mobile Rebuild

## 방향

기존 WebView 내장 웹 번들을 제거하고 React Native 화면으로 다시 구성했습니다. FastAPI/DB/키움/AI 비즈니스 로직은 웹과 동일한 백엔드를 재사용합니다.

## 모바일 네이티브 화면

- 로그인 / 회원가입 / Google·Kakao·Naver 소셜 로그인
- 30문항 투자성향 검사 및 재검사
- 홈 / 시장 현황 / 자산 요약
- 종목 검색 / 종목 상세 / 간단 차트 / 재무 / 뉴스·리포트·공시 / AI 분석
- 스마트 종목 추천 / 인기 테마 / 수급 분석
- 키움 모의투자 연결
- 포트폴리오 / 직접 매수·매도 / 최근 주문
- 가격감시 예약주문
- GBOT 자동매매 상태 / 시작·중지·1회 실행 / 안전한도 설정 / 이력
- 관리자 운영 현황 / 회원 등급·삭제 / 외부 API 설정 및 연결 테스트

## 백엔드 연결

FastAPI 프로세스는 8100입니다. 앱의 기본 API도 `somensomes.iptime.org:8100`입니다.
기존 웹 3000 주소는 8100 직접 접근이 네트워크 단계에서 실패했을 때의 fallback입니다.

## 웹과 동일하지 않은 부분

모바일 UX를 위해 데스크톱의 대형 표/다중열 UI는 카드·세로 목록으로 재설계했습니다. 관리자 진단 ZIP 직접 다운로드와 웹에만 필요한 일부 대형 진단 표는 모바일 핵심 흐름에서 제외했습니다. 해당 서버 API는 변경하지 않았습니다.
