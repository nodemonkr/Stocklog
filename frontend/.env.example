# Leave empty for direct access using the browser hostname and port 8100.
# Example direct public-IP use: frontend at http://203.0.113.10:5174
VITE_BACKEND_PORT=8100

# Optional for HTTPS/reverse proxy or a separate API domain.
# VITE_BACKEND_ORIGIN=https://api.example.com
