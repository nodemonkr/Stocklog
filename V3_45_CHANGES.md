# StockLog v3.45.1 변경사항

## 1. 로그인 / 회원가입 UX 개편
- 로그인 화면을 최신 SaaS 스타일의 2단 레이아웃으로 전면 개편했습니다.
- 모바일/태블릿 반응형 레이아웃을 추가했습니다.
- 회원가입은 `계정 정보 → 투자성향 30문항 → 가입 완료` 3단계로 구성됩니다.
- 투자성향 30문항을 완료하지 않으면 프론트엔드뿐 아니라 백엔드 API에서도 회원가입을 거부합니다.
- 가입 완료 시 투자성향 결과가 `investment_profiles` 테이블에 즉시 저장됩니다.

## 2. AI 일일 사용 횟수
- 일반회원: 하루 5회 무료 (Asia/Seoul 기준 00:00 리셋)
- AI 종목 분석: 종목 1개 분석을 1회로 계산합니다.
- 관리자: 무제한
- 테스트계정: 무제한
- 일별 사용 횟수는 `ai_daily_usage` 테이블에 저장됩니다.
- 사이드바에서 오늘 남은 AI 분석 횟수를 확인할 수 있습니다.
- 추천종목 일괄 AI 분석 시 일반회원은 오늘 남은 횟수만큼의 종목까지만 요청합니다.

## 3. 테스트계정
- `users.is_test_account` 컬럼을 사용합니다.
- 관리자 페이지의 `계정 · AI 사용 관리` 영역에서 일반회원을 테스트계정으로 지정/해제할 수 있습니다.
- 테스트계정은 AI 사용만 무제한이며 `is_admin`은 변경되지 않습니다.
- 관리자 API는 기존과 동일하게 `is_admin=true`인 계정만 접근할 수 있습니다.
- 관리자 계정에는 테스트계정을 중복 부여할 수 없습니다.

## 4. 자동 DB 반영
서버 시작 시 필요한 스키마가 없으면 자동으로 반영합니다.
- `users.is_test_account`
- `ai_daily_usage` 신규 테이블

기존 사용자 및 투자성향 데이터는 삭제하지 않습니다.
