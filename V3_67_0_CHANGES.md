# StockLog v3.67.0

## StockLog Theme Engine
- Gemini를 테마 분류의 필수 요소에서 제거했습니다.
- 키움/인포스탁 원본 테마는 삭제하지 않고 증거 데이터로 보존합니다.
- 공급사 테마, 기존 사업정보, 최근 증권사 리포트, 최근 뉴스, 공식 업종을 가중치로 결합해 고정된 StockLog 표준 테마 체계에 매핑합니다.
- 공식 업종은 낮은 가중치의 참고값으로만 사용합니다. `화학`, `전자`, `기계`처럼 광범위한 업종만으로 투자 테마를 확정하지 않습니다.
- 종목별로 `대표 상위 테마`, `복수 상위 테마`, `세부 테마`, `분류 신뢰도`, `분류 근거`, `엔진 버전`, `근거 JSON`을 저장합니다.

## 상위/세부 테마 계층
- 반도체 > 메모리 반도체 / HBM / 시스템 반도체 / 팹리스 / 파운드리 / 장비 / 소재 / 후공정 등으로 통합합니다.
- 화장품 > K-뷰티 / 화장품 ODM·OEM / 스킨케어 / 색조 등으로 통합합니다.
- 2차전지, 자동차, 디스플레이, 전자부품, 바이오·제약, 로봇, AI·소프트웨어, 방산, 우주항공, 조선, 원전, 전력기기 등 주요 국내 투자 테마도 같은 구조로 표준화했습니다.
- 공급사가 `팹리스`, `메모리 반도체`, `HBM`처럼 서로 다른 이름을 주더라도 Smart Analysis의 상위 필터에서는 모두 `반도체`로 묶입니다.

## 회귀 방지 사례
- 삼성전자: 대표 상위 테마 `반도체`, 세부 `메모리 반도체 / HBM / 파운드리 / 시스템 반도체`.
- 제주반도체: 대표 상위 테마 `반도체`, 세부 `팹리스 / 메모리 반도체 / 시스템 반도체`.
- 코스맥스: 공식 업종 `화학`은 유지하지만 투자 테마는 `화장품`, 세부 `화장품 ODM/OEM / K-뷰티`로 분리합니다.
- 위 대표 사례는 엔진 회귀 테스트에 포함했습니다.

## 테마 필터 수정
- 기존 최대 160개 제한을 제거했습니다.
- 일부 사업 분류 데이터가 존재하면 공급사 테마 전체가 필터에서 사라지던 로직을 제거했습니다.
- 각 종목별로 Theme Engine 결과를 우선하고, 아직 미분류인 종목만 공급사 테마를 표준 체계로 변환해 fallback합니다.
- 대표 테마 목록은 가나다순으로 정렬합니다.
- 대표 테마 선택 시 실제 존재하는 세부 테마를 가나다순으로 추가 필터할 수 있습니다.

## 동기화
- 관리자 `AI로 정리`를 `표준 테마 전체 재구축`으로 교체했습니다.
- Gemini API 키가 없어도 실행됩니다.
- 실시간으로 공급사 표준화 수, 종목 분류 진행량, 표준 테마 저장 수, 낮은 신뢰도, 미분류 수를 표시합니다.
- 전체 동기화에도 `표준 테마` 단계를 추가했습니다.
- 전체 동기화 순서: 시세·재무 → 키움 테마 → 시장 테마 → 종목 분류 → 표준 테마 → 수급 → 스마트 분석 점수.

## DB
`stock_universe`에 다음 컬럼을 자동 마이그레이션합니다.
- `theme_group`
- `theme_groups_json`
- `theme_subthemes_json`
- `theme_engine_version`
- `theme_engine_evidence_json`

기존 `investment_theme`, `investment_themes_json`은 하위 호환용으로 계속 채웁니다.

## 검증
- 백엔드 전체 Python compile 검사 통과.
- 기존 테스트 + 신규 Theme Engine 회귀 테스트 총 30개 통과.
- JSX의 괄호/중괄호/대괄호 정적 검사 통과.
- npm 의존성 설치는 작업 환경의 네트워크 제한으로 완료되지 않아 Vite production build는 실행하지 못했습니다.
